// vitest.setup.ts
import '@testing-library/jest-dom';

// You can add global mocks or setup here if needed.
// For example, if you use next/router, you might want to mock it.
// vi.mock('next/router', () => require('next-router-mock'));

// You can add global mocks or setup here if needed.
// For example, if you use next/router, you might want to mock it.
// vi.mock('next/router', () => require('next-router-mock'));
